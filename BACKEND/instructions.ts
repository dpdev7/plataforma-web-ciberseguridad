/*PARA INSTALAR EL ENTORNO VIRTUAL 

desde la carpeta BACKEND, ejecunten el siguiente comando:

- python -m venv env 

para mac:

- python3 -m venv env

luego de que se les cree la carpeta env se dirigen a la ruta env\Scripts\ y ejecutrar activate y se les activara el entorno virtual, aparecera un (env)

para mac en env/bin/ y ejectutar activate

luego deben instalar django con el siguiente comando:

- pip install django


** PARA CORRER EL SERVIDOR **

desde la carpeta backend, ejecutar el siguiente comando:

python manage.py runserver

el servidor les correra en el puerto 8000*/
